#include<iostream>

using namespace std;

int main() {

	/*
		everything is an object
		object has attributes and methods
		attribute is a variable storage of specific data type and access modifier (private, protected, public)
		data type can be divided into primitive(short, int, long, float, double,char, bool) and composite(pointers, references,strings, arrays) user-defined data types(enum,struct, class)





	
	
	*/

	string name;
	cout << "Enter your name :"<<endl;
	cin >> name;
	cout << "Your name is: " << name << "\n";

	return 0;
}